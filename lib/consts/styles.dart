import 'package:flutter/material.dart';

const InputDecoration appInputStyle = InputDecoration(
  fillColor: Colors.white,
  filled: true,
  border: InputBorder.none,
  hintText: 'Имя сотрудника',
);
